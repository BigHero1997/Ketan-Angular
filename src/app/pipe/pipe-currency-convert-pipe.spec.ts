import { PipeCurrencyConvertPipe } from './pipe-currency-convert-pipe';

describe('PipeCurrencyConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCurrencyConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
